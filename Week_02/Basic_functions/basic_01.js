/* Create a function named add that:
Takes two parameters, a and b.
Returns the sum of a and b.
Call the function with the arguments 10 and 5.
Print the result using console.log(). 

Bonus: Call the function multiple times with different arguments and print all the results.
*/

// Define a function named add that takes two parameters, a and b.
function add(a, b) {
        // The function returns the sum of the two parameters a and b.
    return a + b;
};

// Call the function add with the arguments 10 and 5, and store the result in a variable.
let result1 = add(10, 5);
console.log(result1);

//Calling the function multiple times with different arguments and print all the results.
let result2 = add(15, 10);
console.log(result2);

let result3 = add(24, 7);
console.log(result3);

let result4 = add(28, 15);
console.log(result4);

let result5 = add(55, 35);
console.log(result5);

let result6 = add(4253, 6734);
console.log(result6);